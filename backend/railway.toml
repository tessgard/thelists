[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[deploy.healthcheck]]
path = "/api/health/"
timeout = 100
interval = 30

# Root directory configuration (if railway.toml is in backend folder, this isn't needed)
# But if railway.toml is at repo root, uncomment:
# rootDirectory = "/backend"

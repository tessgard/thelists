# Only override the start command to run migrations at runtime
# Let Nixpacks handle setup, install, and build automatically
[start]
cmd = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn listAPI.wsgi --log-file -"
